# PLAN.md

> PRD.md 기준으로 작성한 구현 계획입니다. 세부 규칙이 궁금하면 항상 [PRD.md](PRD.md)를 먼저 확인하세요.

## 이번 사이클 목표

PRD.md에 정의된 필수 기능(M0~M4)을 모두 구현해, 로그인 없이 접속하면 오늘의 경제 키워드 Top 10과 키워드별 감성 점수를 보여주는 단일 페이지 서비스를 완성하고 Vercel에 배포한다.

## 성공 기준

- 집계 대상 기사 수 1일 50건 이상 (PRD §3)
- Top 10 중 8개 이상이 실제 경제 이슈 키워드, 노이즈 2개 이하 (PRD §3)
- PRD §5의 필수 기능(M0 수집·전처리 / M1 키워드 Top10 / M2 감성 분석 / M3 집계 기준 표시 / M4 예외 상황 안내)이 모두 정상 동작
- Vercel에 배포된 실제 URL에서 위 항목이 확인됨

## 작업 순서

1. Next.js 프로젝트 초기화 (TypeScript + Tailwind CSS + App Router)
2. Python 서버리스 함수 뼈대 `/api/keywords.py` 생성 — 더미 JSON을 반환해 프론트에서 호출되는지 확인
3. `feedparser`로 연합인포맥스 RSS 5개 피드 수집 — 제목·링크·발행시각 추출 ("정책/금융" 피드는 정치·사법 노이즈 때문에 제외)
4. 기사 `link` 기준 중복 제거 로직 추가
5. BeautifulSoup으로 HTML 엔티티 정리 + 제목 앞 대괄호 말머리 제거
6. `pubDate` 기준 최근 24시간 이내(접속 시점 기준 롤링 윈도우) 기사만 필터링
7. RSS 수집 결과에 30분 캐시 적용 (PRD의 "최소 5분" 기준을 충족하면서 서버 부담을 더 줄임)
8. `kiwipiepy` 형태소 분석 연결 — 품사 NNG·NNP·VV·VA·SL만 추출
9. 동사·형용사를 원형(어간+"다")으로 통일
10. 조사·어미·의존명사·1글자 단어 제외 목록 적용
11. 단어 빈도 집계 — 제목당 1회 카운트, 상위 10개 선정
12. `SentiWord_info.json`(KnuSentiLex) 확보 및 코드 전용 로더 작성 (직접 열어 읽지 않음)
13. KnuSentiLex + `econNLPdic.txt` 병합 로직 — 충돌 시 `econNLPdic.txt` 우선, POS/NEG는 +2/−2로 환산
14. 제목별 감성 점수 계산 — 형태소 단위 매칭, 검출된 감성어 점수의 평균
15. 키워드별 감성 점수 계산 및 긍정/중립/부정 판정 (+0.5 이상/−0.5 이하 기준, 미검출은 `-`)
16. API 응답 스키마 확정 — Top 10 배열 + 전체 평균값 + 집계 기준 + 피드 응답 상태
17. Top 10 표 렌더링 (순위 / 키워드 / 등장 기사 수 / 감성 점수)
18. 감성 점수 색상 적용 (긍정=빨강, 부정=파랑, 중립=회색, 미검출은 `-`)
19. 표 오른쪽 위 전체 평균 감성 점수 표시 + 표 아래 판정 기준 범례 추가
20. 화면 상단 집계 기준 표시 (집계 기간(최근 24시간), 집계 기사 수, 마지막 갱신 시각, 정상 응답 피드 수)
20-1. 키워드 클릭 시 해당 기사 제목·원문 링크 목록 펼치기 (PRD §5 M5)
20-2. 순위 변동(▲▼) 표시 (PRD §5 M6)
20-3. 주요 시장 지표 14종 + 1개월 그래프 (PRD §5 M7, `/api/markets.py` + `yfinance`. 환율은 원달러·유로달러·달러엔·달러위안·달러인덱스만)
20-4. 지표-키워드 하이라이트 연동 — Top10에 관련 단어가 있으면 해당 지표 카드 강조 (PRD §5 M7)
20-5. 오늘의 경제 이슈 AI 요약 (PRD §5 M8, `/api/summary.py` + OpenAI `gpt-4o-mini`)
20-6. 키워드 노이즈 제거 — `stopwords.txt`에 "대통령"/"전환" 추가, 정치·사법 기사를 통째로 거르는 `noise_topics.txt` 신설
21. 예외 상황 안내 처리 — 사용자 요청으로 건너뜀
22. 화면 출처 표기 추가 (연합인포맥스, KnuSentiLex, KOSELF 논문)
23. PC 레이아웃 정리 및 모바일 세로 정렬 대응 — 사용자 요청으로 건너뜀
24. Vercel 배포 및 실제 동작 확인 — **완료**. 라이브: https://my-app-wheat-kappa-35.vercel.app / 저장소: https://github.com/Saint-Argentum/daily-economy-top10
