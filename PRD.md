# Daily Economy Top 10 Topics 서비스 기획서 (PRD)

- 작성자: 최성은
- 작성일: 2026-08-03

---

## 1. 추진 배경 (문제 정의)

*누가, 어떤 업무에서, 어떤 불편을 겪는가*

- **누가**: 증권사·은행·자산운용사 등 금융업 실무자와 상경계열 대학생처럼, 경제 지식을 갖추고 매일 시장 흐름을 따라가야 하는 사람들
- **어떤 업무에서**: 매일 아침 시황 파악, 리포트·과제 작성을 위한 사전 조사, 투자 판단을 위한 시장 동향 점검
- **어떤 불편**: 하루에 쏟아지는 경제 기사가 수백 건이라 전부 훑는 것이 현실적으로 불가능하다. 결국 눈에 띄는 몇 건만 골라 읽게 된다. 더 큰 문제는 순서다 — 개별 기사를 여러 개 읽어야만 비로소 큰 틀이 잡히고, 그 틀을 바탕으로 다시 top-down으로 내려가며 해석해야 하기 때문에 비효율적이다.



---

## 2. 현행 방식과 한계

*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

**현행 방식**
- 네이버 금융·연합인포맥스·한국경제 등 경제 매체에 직접 들어가 헤드라인을 위에서부터 훑는다
- 증권사 리서치센터의 데일리 리포트나 모닝브리핑을 받아본다
- 카카오톡 오픈채팅·텔레그램 채널 등에서 공유되는 기사 링크를 본다

**한계**
- **매체가 정한 순서를 그대로 따른다**: 헤드라인 배치는 편집자의 판단이라, 오늘 실제로 가장 많이 다뤄진 이슈와 일치한다는 보장이 없다
- **여러 매체를 봐도 중복을 사람이 걸러야 한다**: 같은 이슈를 매체마다 다르게 써서, 몇 개의 이슈인지 세는 일 자체가 수작업이다
- **리포트는 나오는 시점이 정해져 있다**: 장중에 부각된 이슈는 다음 날 아침에야 반영된다
- **집계된 숫자가 없다**: "오늘 반도체가 유난히 많이 나왔다"는 인상은 남지만, 몇 건인지 근거로 제시할 수 없다



---

## 3. 목표와 기대효과 (성공 기준)

*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

**목표**
- 오늘 경제 뉴스에서 가장 많이 언급된 키워드 Top 10을 **빠르게 파악**할 수 있게 한다
- 키워드마다 등장 기사 수를 함께 제시해 **정량적(quantitative) 증거**로 볼 수 있게 한다. 체감이 아닌 실증적 근거가 제시되므로, 사용자가 더 믿음직스럽게 다음 할 일을 결정할 수 있다

**기대효과**
- 하루 기사 **50건 이상**(RSS 5개 피드 통합)을 자동 집계해, 사람이 읽는 기사 수와 무관하게 전체 그림을 먼저 잡는다
- top-down 순서 회복: 키워드 → 이슈 파악 → 필요한 기사만 선택적으로 읽는 흐름으로 바꾼다
- 키워드별 긍정/부정 표시로, 해당 이슈가 호재로 다뤄지는지 악재로 다뤄지는지 **추가 검색 없이** 판단한다

**성공 기준 (측정 방법)**
- 집계 대상 기사 수 **1일 50건 이상**
- Top 10 중 **8개 이상**이 실제 경제 이슈 키워드 (조사·일반명사 등 노이즈가 2개 이하)



---

## 4. 사용자와 이용 흐름

*누가 쓰는지와 사용 순서 (화살표로)*

**사용자**
- **주 사용자**: 증권사·은행·자산운용사 등 금융업 실무자 — 출근 직후 또는 장 마감 후 시황 파악
- **보조 사용자**: 상경계열 대학생 — 리포트·과제 작성 시 사전 조사
- 로그인 없이 **누구나 접속 즉시** 사용 (별도 가입 절차 없음)

**이용 흐름**

사이트 접속 → 오늘의 키워드 Top 10 표 확인 (순위 + 등장 기사 수 + 감성 값) → 관심 키워드 선별 → 해당 이슈를 다른 채널에서 이어서 조사

**집계 흐름 (내부 동작)**

사용자 접속 → 인포맥스 RSS 5개 피드 수집 → 링크 기준 중복 제거 → HTML 전처리(BeautifulSoup) → 최근 24시간 기사만 필터링 → kiwipiepy 형태소 분석 → 단어 빈도 집계 → 감성 점수 계산 → Top 10 표 출력



---

## 5. 주요 기능

*must(반드시 필요) / nice(있으면 좋음)로 구분해서 작성*

### must (반드시 필요)

**M0. 수집 및 전처리**
- 연합인포맥스 RSS 5개 피드를 `feedparser`로 수집하고, **기사 링크(link) 기준으로 중복을 제거**한다

| 이름 | 카테고리 | URL |
|---|---|---|
| 전체기사 | 전체 | `https://news.einfomax.co.kr/rss/allArticle.xml` |
| 채권/외환 | 채권·외환 | `https://news.einfomax.co.kr/rss/S1N16.xml` |
| 해외주식 | 해외주식 | `https://news.einfomax.co.kr/rss/S1N21.xml` |
| 국제뉴스 | 국제 | `https://news.einfomax.co.kr/rss/S1N23.xml` |
| 증권 | 증권 | `https://news.einfomax.co.kr/rss/S1N2.xml` |

> **"정책/금융"(`S1N15.xml`) 피드는 제외한다.** 원래는 "정책/금융은 시장과 함께 봐야 하는 섹터"라는 이유로 포함했으나, 실제 운영해보니 수사권·탄핵 같은 순수 정치·사법 기사가 이 피드를 통해 다수 유입돼 Top10 노이즈의 주 원인이 되는 게 확인됐다. 사용자 확인 후 피드 자체를 제외하기로 결정했다 (2026-08-05).

- RSS 원문 제목에는 `&quot;` `&amp;` 같은 HTML 엔티티가 섞여 있으므로, **BeautifulSoup으로 전처리**해 일반 문자로 변환한 뒤 분석에 넘긴다 (전처리 없이는 형태소 분석·감성 분석이 모두 어긋난다)
- 제목 앞의 대괄호 말머리(`[증시-마감]`, `[도쿄환시]` 등)를 제거한다
- `pubDate`가 **접속 시점 기준 최근 24시간 이내**인 기사만 집계 대상으로 삼는다 (캘린더 날짜 기준이 아니라, 요청이 들어온 시각에서 24시간을 거슬러 올라간 롤링 윈도우)
- RSS 서버 부담을 줄이기 위해 **최소 5분 간격 캐시**를 두어 과도한 반복 호출을 하지 않는다
- 남은 5개 피드에도 정책·정치 성격의 기사가 섞일 수 있어, 제목에 정치·사법 절차 용어(수사권/탄핵/국회 등)가 있으면 기사 자체를 제외하는 이중 안전장치를 둔다 (`noise_topics.txt`, M0-1 참고)

**M0-1. 정치·사법 기사 제외 (노이즈 필터)**
- 제목에 `noise_topics.txt`의 용어(수사권/탄핵/개헌/검찰/특검/여당/야당/정당/공천/총선/대선/지방선거/국회의원/당대표)가 하나라도 포함되면, 그 기사는 키워드 집계·감성 점수 계산 양쪽에서 완전히 제외한다
- 금리·예산·세제·규제·법안 같은 **금융/재정 정책 용어는 포함하지 않는다** — 그건 이 앱이 다뤄야 할 진짜 경제 이슈이기 때문
- `stopwords.txt`(개별 단어만 제외)와는 다른 층위다 — 이건 기사 전체를 제외한다

**M1. 오늘의 키워드 Top 10 표**
- 전처리된 최근 24시간 기사 제목에서 단어 빈도를 집계해 상위 10개를 표로 출력한다
- 결과를 **하나의 표**로 출력하며, 컬럼 구성은 다음과 같다:

| 순위 | 키워드 | 등장 기사 수 | 감성 점수 |
|---|---|---|---|
| 1 | 반도체 | 24 | <span style="color:#D32F2F">+0.82</span> |
| 2 | 금리 | 19 | <span style="color:#1565C0">-0.61</span> |
| 3 | 환율 | 15 | <span style="color:#757575">+0.12</span> |

- 감성 라벨(긍정/부정) 컬럼은 **따로 만들지 않고**, 감성 점수 숫자 자체에 색을 입혀 표현한다
- kiwipiepy로 명사·동사(원형)·형용사·외국어만 추출하고, 한 제목당 같은 단어는 1회만 카운트한다
- 24시간 롤링 윈도우라 정해진 초기화 시각은 없다 — 매 요청마다 그 시점 기준 최근 24시간으로 다시 계산된다

**M2. 키워드 감성 분석**

*사용 사전*
- **KnuSentiLex**(범용, 14,843개) + **econNLPdic.txt**(재무 특화, 60개)를 **병합해서 사용**한다
- `econNLPdic.txt`는 KOSELF 논문(조수지·김흥규·양철원, 한국증권학회지 2021) `<표 3>`의 상위 빈도 단어 60개(부정 30 + 긍정 30)를 정리한 파일이다. KOSELF 전체 목록(95개)의 공식 배포 페이지는 현재 접속되지 않으므로, **논문에서 확인 가능한 60개를 확정 범위로 삼는다**
- KnuSentiLex 단독으로는 `상승·하락·감소·확대·적자·둔화·악화·손실·회복·하향`이 모두 미수록이라 경제 뉴스에서 제 기능을 못 한다. `econNLPdic.txt` 60개 중 **42개가 KnuSentiLex 미수록어**이며, 이들이 그 빈 곳을 메운다
- 두 사전에서 같은 단어의 극성이 다르면 **재무 특화인 `econNLPdic.txt`를 우선**한다
- `econNLPdic.txt`의 극성은 POS/NEG 두 값이므로, 점수 계산 시 **POS = +2, NEG = -2**로 환산한다

*도메인 차이 주의*
- `econNLPdic.txt`는 **애널리스트 보고서**를 기준으로 만들어진 사전이라, 뉴스 제목에서는 맥락이 달라지는 단어가 있다
- 아래 단어는 **1차 구현에서 제외 후보**로 두고, 실제 순위를 확인한 뒤 유지 여부를 결정한다
  - 정도부사·중립어로 쓰이는 경우가 많음: `이상`, `매우`, `진행`, `수치`, `결국`
  - 보고서 특유 맥락에 의존: `전환`, `제외`, `매각`, `처리`, `공격`, `바닥`

*매칭 방식*
- 감성어는 **형태소 단위로 매칭**한다. 문자열 부분 일치를 쓰지 않는다
- (부분 일치를 쓰면 `"1,420원대 지지력"`의 '지지'가 부정어로 잡히는 등 오탐이 발생한다)

*점수 계산*
- 감성어 원점수는 **-2 ~ +2 정수**를 사용한다
- **제목 1건의 점수 = 그 제목에서 검출된 감성어 점수들의 평균** (합이 아닌 평균 → 항상 -2 ~ +2 범위 유지)
- **키워드의 점수 = 그 키워드가 등장한 제목들의 평균**
- 감성어가 하나도 검출되지 않은 제목은 평균 계산에서 **제외**한다 (0점으로 넣지 않는다)

*표시*
- 판정 기준: **+0.5 이상 긍정 / -0.5 이하 부정 / 그 사이 중립**
- 감성 점수는 소수점 **둘째 자리**까지 표시한다
- 색상은 국내 증시 관례를 따라 **긍정=빨강, 부정=파랑, 중립=회색**으로 한다
- 감성어가 전혀 검출되지 않은 키워드는 0.00이 아니라 **`-`로 표시**한다 (계산 결과 중립인 것과, 판단 불가인 것을 구분한다)
- 표 **오른쪽 위**에 Top 10 키워드의 **전체 평균 감성 점수**를 `평균값: +0.11` 형태로 표시한다
- 표 **아래에 작은 폰트**로 판정 기준 범례를 넣는다 — 예: *「+0.5 이상 긍정 · -0.5 이하 부정 · 그 사이 중립 · `-`는 감성어 미검출」*

**M3. 집계 기준 표시**
- 화면 상단에 집계 기간(최근 24시간, 접속 시점 기준), 집계 기사 수, 마지막 갱신 시각(RSS를 실제로 수집한 시각 — 캐시 때문에 접속 시각과 다를 수 있음)을 표시한다 (숫자의 신뢰성 확보)
- **정상 응답한 RSS 피드 수**(예: `5개 중 4개 수집`)를 함께 표시한다. 일부 피드에 장애가 생기면 편향된 순위가 나올 수 있으므로 사용자가 이를 알 수 있어야 한다

**M4. 예외 상황 안내**
- 최근 24시간 기사가 **집계 최소 기준에 못 미치면** 표 대신 *"집계할 기사가 아직 충분하지 않습니다"* 안내를 표시한다 (24시간 롤링 윈도우라 평상시엔 거의 발생하지 않고, RSS 피드 장애나 서비스 최초 기동 직후 정도에만 해당)
- **주말·공휴일**에는 기사량이 평일보다 크게 적으므로, *"휴장일이라 기사 수가 적습니다"* 안내를 함께 표시한다

**M5. 키워드별 기사 목록 펼치기**
- 표에서 키워드 행을 누르면 그 아래로 해당 키워드가 등장한 **기사 제목 목록**이 펼쳐진다
- 각 제목은 원문 기사로 가는 링크이며, 새 탭에서 열린다 (본문을 앱 안에서 보여주지 않는다 — 저작권상 제목과 링크만 사용한다는 §7 원칙 유지)
- 한 번에 하나의 키워드만 펼쳐지고, 다시 누르면 접힌다

**M6. 순위 변동 표시**
- 직전 RSS 수집 시점(최대 30분 전) 대비 순위가 오르면 ▲, 내리면 ▼, 그대로면 `–`, 새로 Top10에 든 키워드는 `NEW`로 표시한다
- DB 없이 서버리스 함수 메모리에만 직전 순위를 들고 있는다 (PRD: 영구 저장 없음). 함수가 재시작되면 초기화된다

**M7. 주요 시장 지표**
- 키워드 표 아래에 지수·금리·환율·원자재 16종을 카드 + 최근 1개월 선 그래프로 보여준다: KOSPI, KOSDAQ, 나스닥, 유로스탁스50, 필라델피아반도체(SOX), VIX, 미국채 10년물, 달러인덱스(DXY), WTI유, 브렌트유, 원/달러, 달러/엔, 유로/달러, 엔/원(100엔 기준), 유로/원, 위안/원
- 데이터 출처는 `yfinance`(야후 파이낸스 비공식 라이브러리, API 키 불필요). 별도 서버리스 함수(`/api/markets.py`)에서 담당하며, `/api/keywords.py`와는 무관하다
- 종목 구성은 국내 증권사 리서치센터가 아침 시황을 해석하는 순서(미국 금리 → 달러인덱스 → 원/달러 → S&P500/나스닥 → SOX → 원자재)를 참고했다
- 아래 종목은 실제로 확인해본 결과 yfinance로 확보할 수 없어 **제외**했다:
  - 미국채 1년물 — 현물·선물 지수 모두 없음
  - 미국채 2년물 — 현물 지수가 없다 (선물 `2YY=F`로 대체 가능하나, 정확도 차이가 있어 제외하기로 함)
  - 한국 국채 1년/2년/10년물 — yfinance는 한국 국채 **수익률(%)** 자체를 취급하지 않는다 (국채 ETF 가격은 있으나 수익률과는 다른 지표라 대상에서 뺐다)
- 위안/원은 직접 크로스(`CNYKRW=X`)가 야후에 하루치만 있어서, 데이터가 온전한 `KRWCNY=X`(원 기준 위안화)를 받아 역수를 취해 계산한다
- RSS와 마찬가지로 DB에 쌓지 않고 30분 캐시만 두고 매번 다시 받아온다. 야후 응답 형식이 바뀌거나 접근이 막히면 해당 종목만 "불러오기 실패"로 표시하고 나머지는 정상 표시한다
- 각 카드에는 **전일 대비 변동**도 함께 표시한다 — 금리(%) 종목은 **bp**(1bp=0.01%p) 단위로, 그 외(지수·환율·원자재)는 **pt/원/달러** 단위로 표시하며 국내 증시 관례대로 상승은 빨강·하락은 파랑으로 색을 입힌다
- 각 카드를 누르면 해당 종목의 **야후 파이낸스 페이지**가 새 탭으로 열린다 (위안/원은 우리가 표시하는 방향과 같은 `CNYKRW=X` 페이지로 연결 — 계산에 쓰는 `KRWCNY=X`로 보내면 화면 숫자와 안 맞아 보여서 헷갈린다)
- 오늘의 키워드 Top10에 특정 지표와 관련된 단어(예: "환율", "코스닥", "금리")가 있으면 해당 카드에 시각적 강조(테두리+배지)를 준다 — 완전 자동 매칭이 아니라 미리 정해둔 별칭 목록 기준이다

**M8. 오늘의 경제 이슈 AI 요약**
- Top10 표 위쪽에 오늘의 키워드·시장 지표를 근거로 LLM이 작성한 3~5문장 요약을 보여준다
- **이 기능은 PRD의 "LLM 기반 요약 없음" 원칙에 대한 명시적 예외다** (사용자 요청으로 추가, 2026-08-04). 키워드 감성 점수 계산 자체는 여전히 사전 매칭만 쓰고, 이 요약은 이미 계산된 결과를 설명하는 용도로만 LLM을 쓴다
- 별도 서버리스 함수 `/api/summary.py`가 담당한다. 뉴스나 시세를 다시 수집하지 않고, 프론트가 `/api/keywords`·`/api/markets`에서 이미 받아온 데이터를 그대로 넘겨받아 요약만 만든다
- 모델은 `gpt-4o-mini`, `OPENAI_API_KEY`는 `.env`(로컬) 또는 Vercel 프로젝트 환경변수(배포 후)에서 읽는다 — 이 프로젝트에서 실제 앱 런타임이 처음으로 LLM API를 호출하는 지점이다
- 프롬프트에는 국내 증권사가 실제로 쓰는 매크로 해석 원칙(금리↑→긴축 부담, 달러/DXY↑→원화 약세, 나스닥·SOX↑→반도체 심리 개선, 유가↑→인플레 부담 등)을 시스템 지침으로 넣어, 오늘 실제 수치를 그 틀로 설명하게 한다
- 특정 종목 매수·매도는 권유하지 않는다 — 화면에도 "투자 조언이 아닙니다" 문구를 always 표시한다
- 30분 캐시를 둔다 (keywords.py/markets.py와 같은 주기). 매 요청마다 LLM을 부르면 비용·응답시간이 늘어나기 때문

### nice (있으면 좋음)

- 표 컬럼별 정렬 (등장 기사 수 순 ↔ 감성 점수 순 전환)
- 키워드 워드클라우드
- 카테고리별(증권 / 채권·외환 / 국제 등) 키워드 분리 보기



---

## 6. 범위와 비범위

*이번 실습에서 만들 것 / 만들지 않을 것*

**범위 (이번 실습에서 만들 것)**
- 연합인포맥스 RSS 5개 피드 수집, 링크 기준 중복 제거, BeautifulSoup 전처리
- 최근 24시간(접속 시점 기준 롤링 윈도우) 기사 필터링
- kiwipiepy 형태소 분석 기반 키워드 빈도 집계 (명사·동사 원형·형용사·외국어)
- KnuSentiLex + KOSELF 병합 사전 기반 키워드별 감성 점수 산출
- Top 10 표 + 전체 평균값 + 집계 기준 + 예외 상황 안내가 표시되는 단일 페이지 화면
- PC 브라우저 기준 레이아웃 (모바일은 세로 정렬만 대응)

> **참고**: RSS 5개 피드를 한 번만 호출해도 최근 24시간 기사가 대부분 확보되므로(실측: 16:40 시점 07:00~16:34 기사 90건, 시간대 공백 없음), **별도의 누적 저장 없이 요청 시점에 수집하는 방식**으로 충분하다. 다만 각 피드는 최신 항목을 일정 개수만 유지하므로, 뉴스가 몰리는 시간대 직후에는 24시간 전 경계에 가까운 기사 일부가 피드 창에서 이미 밀려났을 수 있으며, 이번 실습에서는 이를 감수한다.

**비범위 (이번에 만들지 않을 것)**
- 회원가입·로그인, 개인화, 즐겨찾기, 알림
- 기사 본문 열람 화면 (키워드별 기사 **목록**은 범위에 포함 — 표에서 키워드를 누르면 제목과 원문 링크가 펼쳐진다. 본문은 앱 안에서 보여주지 않고 링크로 원 매체에 보낸다)
- 뉴스 본문 크롤링 (기사 **제목만** 사용)
- 경제 외 카테고리(정치·사회·IT 등) 확장
- 과거 날짜 조회 및 키워드 추이 그래프
- 외부 DB(PostgreSQL, Supabase 등) 연동 및 데이터 영구 저장 (시장 지표도 DB에 쌓지 않고, 매 요청마다 yfinance에서 최근 1개월치를 다시 받아온다)
- LLM 기반 **감성 판정**은 여전히 하지 않는다 — 키워드 감성 점수(§5 M2)는 지금도 오직 KnuSentiLex+econNLPdic 사전 매칭으로만 계산한다. (LLM 기반 **이슈 요약**은 사용자 요청으로 M8에서 예외로 추가했다 — 아래 참고)
- 다국어 지원 (한국어 기사만 대상)



---

## 7. 보안·개인정보 검토

*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

**사용 문서의 공개등급**
- **연합인포맥스 RSS**: 공개 등급. 로그인·인증 없이 접근 가능한 공개 배포 피드
- **KnuSentiLex 감성사전**: 공개 등급. GitHub 공개 저장소 배포, 비영리·연구 목적 사용 허용 → **출처 명시 필수**
- **econNLPdic.txt (KOSELF 기반)**: 공개 등급. 한국증권학회지(2021) 게재 논문 `<표 3>`에 수록된 단어 목록 → **출처 명시 필수** (조수지·김흥규·양철원, 2021)
- 수집 대상은 이미 공개된 보도 기사의 제목뿐이며, 유료·내부 자료는 사용하지 않는다

**개인정보 여부**
- **수집하는 개인정보 없음**: 로그인·회원가입·사용자 입력이 없고, 쿠키·행동 추적도 하지 않는다
- 기사 제목에 공인의 실명(기업 대표, 정책 결정자 등)이 포함될 수 있으나 이는 공개 보도된 내용의 일부이며, 별도로 인물 정보를 수집·결합하지 않는다
- 처리 과정에서 다루는 데이터는 **제목·링크·발행시각**뿐이며, 개인 식별정보는 저장하지 않는다

**API 키 관리**
- RSS·감성사전·형태소 분석기·시장 지표(`yfinance`)는 API 키가 필요 없다
- **예외**: M8(AI 이슈 요약)은 `OPENAI_API_KEY`로 OpenAI API를 호출한다 — 이 프로젝트에서 실제 앱 런타임이 쓰는 유일한 API 키다. `.env`(로컬)와 Vercel 프로젝트 환경변수(배포)에서 읽으며, 코드에 하드코딩하지 않는다
- `.env` + `.gitignore` 체계를 유지해 키가 저장소에 커밋되지 않도록 한다
- 키가 필요해질 경우 원칙
  1. 코드에 하드코딩하지 않는다
  2. `.env`에만 보관하고 저장소에 커밋하지 않는다
  3. 배포 시 플랫폼 환경변수로 주입한다
  4. 노출이 의심되면 즉시 폐기하고 재발급한다

**저작권 및 이용 조건**
- 기사 **제목과 링크만** 사용하고 본문은 저장·재배포하지 않는다
- 화면에 출처(연합인포맥스)와 사용 사전 출처(KnuSentiLex, KOSELF 논문)를 명시한다
- RSS 서버에 과도한 반복 호출을 하지 않는다 (캐시 규칙은 5번 M0 참조)



---

## 8. 기술 스택

*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

**프론트엔드**
- Next.js (App Router) — 과정 지정 프레임워크
- TypeScript
- Tailwind CSS — 표 스타일링 및 감성 점수 색상 처리

**백엔드 (데이터 처리)**
- Python Serverless Function 3개 — Vercel Python Runtime
  - `/api/keywords.py` — 뉴스 키워드·감성 분석 (M0~M6)
  - `/api/markets.py` — 시장 지표(M7). keywords.py와 무관한 별도 함수
  - `/api/summary.py` — AI 이슈 요약(M8). keywords.py/markets.py가 이미 수집한 데이터를 POST로 받아 요약만 생성
- `feedparser` — 연합인포맥스 RSS 5개 피드 수집
- `beautifulsoup4` — HTML 엔티티 및 태그 전처리
- `kiwipiepy` — 한국어 형태소 분석 (명사·동사 원형·형용사·외국어 추출)
- 감성 사전 — `SentiWord_info.json` (KnuSentiLex, 범용 14,843개) + `econNLPdic.txt` (KOSELF 기반, 재무 특화 60개)
- `yfinance` — 시장 지표(M7) 수집, 야후 파이낸스 비공식 라이브러리
- `openai` — AI 이슈 요약(M8) 생성, `gpt-4o-mini` 모델 사용

**데이터 저장**
- 별도 DB 미사용, 파일 누적 저장도 하지 않는다
- 요청 시점에 RSS를 수집해 **메모리에서 처리**하고, 30분 캐시로 반복 호출만 줄인다

**배포·개발 환경**
- Vercel — Next.js + Python Runtime 동시 지원
- Git / GitHub — 버전 관리 (`.env`는 `.gitignore`로 제외)
- Claude Code — 개발 도구

**비고**
- 형태소 분석은 `kiwipiepy`를 사용한다. (`konlpy`/Okt는 Java 의존성 때문에 서버리스 환경에서 동작하지 않는다)
- 감성 분석은 `SentiWord_info.json` + `econNLPdic.txt` 사전 기반으로 수행하며 외부 LLM API는 사용하지 않는다.
- 감성사전 파일은 **저장소에 포함**해 배포한다. 런타임에 외부에서 내려받지 않는다.
- `SentiWord_info.json`(약 15,000줄)은 **AI 코딩 도구가 직접 열어 읽지 않는다.** 코드로만 로드해 사용한다. (전체를 읽으면 불필요한 토큰이 크게 소모된다) `econNLPdic.txt`는 60줄이므로 읽어도 무방하다.
- 단기 실습 프로젝트이므로 영구 저장소는 구축하지 않았으며, 실제 서비스에서 과거 데이터 축적이 필요해지면 데이터베이스(Supabase, PostgreSQL 등)를 사용하는 것이 적절하다.

---

## 9. 개발 단위

*핵심 기능을 만들기 좋은 작은 단위로 나눈 목록 (만들 순서대로)*

1. Next.js 프로젝트 초기화 — TypeScript + Tailwind + App Router
2. Python 서버리스 함수 뼈대 `/api/keywords.py` 생성 — 고정값 JSON을 반환하고 프론트에서 호출되는지 확인
3. `feedparser`로 RSS 5개 피드 수집 — 제목·링크·발행시각 추출
4. 기사 링크(link) 기준 중복 제거
5. BeautifulSoup으로 HTML 엔티티 전처리 및 대괄호 말머리 제거
6. `pubDate` 기준 최근 24시간 이내(접속 시점 기준 롤링 윈도우) 기사만 필터링
7. 수집 결과 30분 캐시 적용
8. `kiwipiepy` 형태소 분석 연결 — 품사 NNG·NNP·VV·VA·SL만 추출
9. 동사·형용사를 원형("어간 + 다")으로 변환
10. 불용어·의존명사·1글자 단어 제외 목록 적용
11. 단어 빈도 집계 — 제목당 1회 카운트, 상위 10개 선정
12. 감성사전 로드 — `SentiWord_info.json` + `econNLPdic.txt` 병합, 충돌 시 `econNLPdic.txt` 우선
13. 제목별 감성 점수 계산 — 형태소 단위 매칭, 검출된 감성어 점수의 평균
14. 키워드별 감성 점수 계산 및 긍정·중립·부정 판정 (±0.5 기준, 미검출은 `-`)
15. API 응답 스키마 확정 — Top 10 배열 + 전체 평균값 + 집계 기준 + 피드 응답 상태
16. Top 10 표 렌더링 (순위 / 키워드 / 등장 기사 수 / 감성 점수)
17. 감성 점수 색상 적용 — 긍정=빨강, 부정=파랑, 중립=회색, 미검출은 `-`
18. 표 오른쪽 위 전체 평균값 표시 및 표 아래 판정 기준 범례 추가
19. 화면 상단 집계 기준 표시 — 집계 기간(최근 24시간), 집계 기사 수, 갱신 시각, 정상 응답 피드 수
20. 예외 상황 안내 표시 — 기사 부족, 휴장일(주말·공휴일)
21. 화면 출처 표기 — 연합인포맥스, KnuSentiLex, KOSELF 논문
22. PC 레이아웃 정리 및 모바일 세로 정렬 대응
23. Vercel 배포 및 동작 확인


