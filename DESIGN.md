# DESIGN.md

> [PLAN.md](PLAN.md)의 작업 순서를 기준으로 작성한 설계 문서입니다. 세부 규칙은 항상 [PRD.md](PRD.md)를 따릅니다.
>
> **배포 완료**: https://my-app-wheat-kappa-35.vercel.app (저장소: https://github.com/Saint-Argentum/daily-economy-top10)

## 1. 화면 구성 (무엇이 어디에 보이는지)

PC 기준 가로 1200px 내외 한 페이지, 모바일에서는 아래 블록들이 세로로 쌓입니다.

```
┌─────────────────────────────────────────────┐
│ ① 상단 정보 바                                │
│   집계 기간(최근 24시간) · 집계 기사 수         │
│   · 마지막 갱신 시각 · 정상 응답 피드 수         │
│   (예: 5개 중 4개 수집)                        │
├─────────────────────────────────────────────┤
│ ② 예외 상황 안내 (해당할 때만 표시)             │
│   "집계할 기사가 아직 충분하지 않습니다" 또는     │
│   "휴장일이라 기사 수가 적습니다"               │
├─────────────────────────────────────────────┤
│ ③ Top 10 표                    평균값: +0.11 │
│  ┌────┬────────┬──────────┬──────────┐     │
│  │순위 │ 키워드  │ 등장 기사수│ 감성 점수 │     │
│  ├────┼────────┼──────────┼──────────┤     │
│  │ 1  │▾반도체  │   24     │  +0.82   │     │
│  ├────┴────────┴──────────┴──────────┤     │
│  │ · 반도체 수출 급증…(원문 링크)        │     │
│  │ · 반도체특별법 시행령 의결…(원문 링크) │     │
│  ├────┬────────┬──────────┬──────────┤     │
│  │ 2  │▸금리    │   19     │  -0.61   │     │
│  │... │  ...    │  ...     │   ...    │     │
│  └────┴────────┴──────────┴──────────┘     │
│  범례(작은 글씨): +0.5 이상 긍정(빨강) ·        │
│  -0.5 이하 부정(파랑) · 그 사이 중립(회색) ·     │
│  `-`는 감성어 미검출                          │
├─────────────────────────────────────────────┤
│ ④ 출처 표기 (하단, 작은 글씨)                   │
│                                            │ 
└─────────────────────────────────────────────┘
```

- 메인 색상은 남색(네이비) 계열, 장식 요소 최소화. 표 하나가 화면의 중심입니다.
- 감성 점수는 별도 라벨 컬럼 없이 숫자 자체에 색을 입혀 표현합니다 (긍정=빨강 / 부정=파랑 / 중립=회색).
- ②(예외 상황 안내)는 사용자 요청으로 이번에는 구현하지 않습니다.
- ③의 키워드 행을 누르면 그 아래로 해당 기사 제목 목록이 펼쳐지고, 각 제목은 원문 기사 링크입니다 (새 탭에서 열림). 한 번에 하나만 펼쳐집니다.
- ③ 순위 컬럼에는 직전 RSS 수집 대비 순위 변동(▲▼/–/NEW)도 함께 표시됩니다 (PRD §5 M6).
- **⑤ 오늘의 경제 이슈 AI 요약** — ①(상단 정보 바) 바로 아래, ③(Top10 표) 위쪽에 배치됩니다. LLM이 Top10 키워드·시장 지표를 바탕으로 쓴 3~5문장 요약과 "투자 조언이 아닙니다" 안내가 함께 나옵니다 (PRD §5 M8).
- ③ 아래에 **⑥ 주요 시장 지표** 블록이 추가됩니다 — KOSPI·KOSDAQ·나스닥·유로스탁스50·필라델피아반도체(SOX)·VIX·미국채10년물·달러인덱스·WTI유·브렌트유·원달러·달러엔·유로달러·엔원·유로원·위안원 16개를 카드 + 최근 1개월 선 그래프로 보여줍니다 (PRD §5 M7). Top10에 관련 키워드(예: "코스피", "환율")가 있으면 해당 카드에 빨간 테두리+배지로 강조합니다.

## 2. 데이터 흐름 (입력 → 처리 → 출력)

```
[사용자가 페이지 접속]
        │
        ▼
Next.js 페이지가 /api/keywords 호출
        │
        ▼
┌───────────────── Python 서버리스 함수 ─────────────────┐
│ 1. 30분 캐시 확인 → 있으면 캐시된 결과 바로 반환         │
│ 2. 연합인포맥스 RSS 5개 피드 수집 (feedparser)         │
│ 3. 기사 링크(link) 기준 중복 제거                      │
│ 4. HTML 엔티티 정리 + 대괄호 말머리 제거 (BeautifulSoup)│
│ 5. 최근 24시간 이내 기사만 필터링                      │
│ 5-1. 정치·사법 기사 통째로 제외 (noise_topics.txt)      │
│ 6. 형태소 분석 (kiwipiepy) → 명사/동사원형/형용사/외래어│
│ 6-1. 불용어·1글자 제외 (stopwords.txt)                 │
│ 7. 단어 빈도 집계 → 상위 10개 선정                     │
│ 8. 감성사전(KnuSentiLex + econNLPdic) 로드 및 병합     │
│ 9. 제목별 감성 점수 → 키워드별 감성 점수 계산           │
│ 10. 응답 JSON 조립 (Top10 + 평균값 + 집계 기준 + 피드 상태)│
└─────────────────────────────────────────────────────┘
        │
        ▼
Next.js 페이지가 JSON을 받아 화면(①~④)에 렌더링
```

- 어떤 데이터도 DB나 파일에 쌓이지 않습니다. 매 요청마다 위 과정을 (30분 캐시 범위 안에서) 새로 실행하고, 응답을 만든 뒤 버립니다.
- 감성사전 파일(`SentiWord_info.json`, `econNLPdic.txt`)은 저장소에 포함되어 배포되며, 외부에서 다운로드하지 않습니다.
- 시장 지표(⑥)는 완전히 별개의 흐름입니다: Next.js 페이지가 `/api/markets`도 함께 호출하고, 그 함수는 `yfinance`로 16개 종목의 최근 1개월 데이터를 받아 30분 캐시 후 반환합니다. `/api/keywords`와는 서로 의존하지 않아 한쪽이 느리거나 실패해도 다른 쪽엔 영향이 없습니다.
- AI 요약(⑤)은 위 두 흐름이 끝난 *뒤에* 이어집니다: 프론트가 `/api/keywords`·`/api/markets` 응답을 받으면, 그 결과를 그대로 `/api/summary`에 POST로 넘깁니다. `summary.py`는 뉴스나 시세를 다시 수집하지 않고 OpenAI(`gpt-4o-mini`)를 호출해 요약 문장만 만들어 30분 캐시합니다.

## 3. 기술 선택

기본 뼈대는 PRD에서 정한 대로 **Next.js**로 고정합니다. 오늘 안에 Vercel로 배포하는 것이 목표이므로, Next.js가 기본적으로 제공하는 기능 위에 아래 기술만 최소한으로 추가합니다.

| 기술 | 역할 | 쉬운 설명 (비개발자용) |
|---|---|---|
| **Next.js (App Router)** | 화면 전체의 기본 뼈대 | 웹사이트의 뼈대를 잡아주는 도구. 페이지 이동, 화면 그리기를 다 알아서 해줍니다. |
| **TypeScript** | Next.js 코드 작성 언어 | 자바스크립트에 "이 값은 숫자, 이건 글자"처럼 규칙을 붙여서, 실수를 미리 잡아주는 언어입니다. |
| **Tailwind CSS** | 화면 꾸미기 | 표 테두리, 색깔, 여백 같은 디자인을 빠르게 입히는 도구입니다. |
| **Python 서버리스 함수** (`/api/keywords.py`, Vercel Python Runtime) | 뉴스 수집·분석 담당 | 화면은 Next.js가, 데이터 계산은 파이썬이 나눠서 맡습니다. 주방(파이썬)에서 요리하고 홀(Next.js)에서 손님에게 내어주는 것과 비슷합니다. Vercel이 둘 다 같이 배포해줍니다. |
| **feedparser** | RSS 뉴스 목록 읽기 | 연합인포맥스 사이트에 사람이 직접 들어가지 않아도, 새 기사 목록을 자동으로 받아오는 도구입니다. |
| **beautifulsoup4** | 기사 제목 글자 정리 | 제목에 섞인 `&quot;` 같은 깨진 부호를 원래 문자(")로 되돌려주는 도구입니다. |
| **kiwipiepy** | 한국어 단어 추출 | 문장에서 "무엇이 명사인지, 무엇이 동사인지"를 구분해주는 한국어 전용 도구입니다. 이 프로젝트에서 키워드를 뽑아내는 핵심 부품입니다. |
| **KnuSentiLex + econNLPdic.txt (감성사전 파일)** | 긍정/부정 판정 | 미리 만들어진 "이 단어는 긍정, 이 단어는 부정" 목록입니다. AI가 새로 판단하는 게 아니라, 이 목록과 단어를 대조만 해서 점수를 매깁니다. |
| **yfinance** (`/api/markets.py`) | 시장 지표(지수·금리·환율·원자재) 수집 | 야후 파이낸스에서 주가·금리·환율 데이터를 인증 없이 가져오는 도구입니다. keywords.py와는 별개 함수로 분리해뒀습니다. |
| **openai** (`/api/summary.py`) | 오늘의 경제 이슈 AI 요약 | 이미 계산된 키워드·시장 지표 숫자를 자연어 문장으로 풀어 설명해주는 도구입니다. `gpt-4o-mini` 모델을 씁니다. 이 프로젝트에서 유일하게 실제 API 키(`OPENAI_API_KEY`)가 필요한 부분입니다. |
| **Vercel** | 배포·호스팅 | 코드를 올리면 인터넷에서 바로 접속 가능한 주소로 만들어주는 서비스입니다. 오늘 중 배포가 가능한 이유입니다. |

- DB(Supabase, PostgreSQL 등)는 사용하지 않습니다. PRD에서 정한 대로 데이터를 쌓아두지 않기 때문입니다.
- 다른 프레임워크로의 전환이나 마이그레이션은 제안하지 않습니다 (CLAUDE.md 작업 규칙).
