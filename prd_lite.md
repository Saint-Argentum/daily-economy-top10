# Daily Economy Top 10 Topics 서비스 기획서 (PRD_lite)

- 작성자: 최성은
- 작성일: 2026-08-03

> 이 문서는 요약본입니다. **구현 기준은 [PRD.md](PRD.md)를 따릅니다.**

---

## 1. 이 앱은 한 마디로 무엇인가요?
- 답변: 매일 00시를 기준으로, 그날 연합인포맥스 경제 뉴스 RSS에 올라온 기사 제목들을 모아 가장 많이 등장한 단어 Top 10과 각 단어의 감성 점수를 표로 보여주는 서비스 (한 기사 제목 내 중복 단어는 1회만 카운트)

---

## 2. 누가 쓰고, 왜 쓰나요? (딱 한 줄씩만!)
- 누가 쓰나요?
  - 답변: 금융업 종사자, 상경계열 대학생 등 경제 지식이 있는 사람들
- 어떤 불편을 해결하나요?
  - 답변: 경제 뉴스를 하나하나 읽지 않아도 오늘의 핵심 키워드를 빠르게 파악해, 시장 흐름 예측이나 투자·업무 판단에 걸리는 시간을 줄여준다

---

## 3. 개발할 핵심 기능 (딱 2가지만!)
> 💡 너무 많은 기능을 만들려고 하면 AI가 코드를 꼬아버립니다.
> 가장 중요한 2개 기능만 "규칙"을 정해 알려주세요.

### 1) 오늘의 경제 키워드 Top 10
- 설명: 연합인포맥스 RSS 6개 피드에서 기사 제목을 수집하고, kiwipiepy 형태소 분석으로 실질 의미를 가진 단어만 추출해 빈도를 집계한 뒤 상위 10개를 순위로 보여준다
- AI가 꼭 지켜야 할 규칙:
  - 뉴스 수집은 `feedparser`로 아래 연합인포맥스 RSS 6개를 읽는다 (별도 API 키 불필요)
    - 전체기사 `https://news.einfomax.co.kr/rss/allArticle.xml`
    - 정책/금융 `https://news.einfomax.co.kr/rss/S1N15.xml`
    - 채권/외환 `https://news.einfomax.co.kr/rss/S1N16.xml`
    - 해외주식 `https://news.einfomax.co.kr/rss/S1N21.xml`
    - 국제뉴스 `https://news.einfomax.co.kr/rss/S1N23.xml`
    - 증권 `https://news.einfomax.co.kr/rss/S1N2.xml`
  - 요청 시점에 6개 피드를 읽어 **메모리에서 처리**한다. 파일 누적 저장은 하지 않는다 (한 번 호출로 최근 24시간 기사가 대부분 확보된다)
  - RSS 서버 부담을 줄이기 위해 **최소 5분 간격 캐시**를 둔다
  - 전체기사 피드와 카테고리 피드는 기사가 겹치므로 **기사 링크(link) 기준으로 중복을 제거**한다
  - RSS 제목에 섞인 HTML 엔티티(`&quot;`, `&amp;` 등)는 **BeautifulSoup으로 전처리**해 일반 문자로 바꾼 뒤 분석한다
  - 제목 앞의 대괄호 말머리(`[증시-마감]`, `[도쿄환시]` 등)는 제거한 뒤 분석한다
  - 기사의 `pubDate`가 접속 시점 기준 최근 24시간 이내인 기사만 집계 대상으로 삼는다 (캘린더 날짜 자정 기준 아님)
  - 한 기사 제목 안에서 같은 단어가 여러 번 나와도 1회만 카운트한다
  - 형태소 분석기는 kiwipiepy를 사용한다 (konlpy/Okt는 Java 의존성 때문에 쓰지 않는다)
  - 추출 대상 품사는 명사(NNG, NNP), 동사(VV), 형용사(VA), 외국어(SL)로 한정한다
  - 동사·형용사는 어간에 "다"를 붙인 **원형**으로 통일해 집계한다 (예: 올랐다 / 오르며 / 올라 → 오르다)
  - 조사·어미·의존명사(것/수/등/때 등)와 1글자 단어는 제외한다
  - 정책·정치 성격의 기사가 일부 섞이지만 **걸러내지 않는다** (정책/금융은 시장과 함께 봐야 하는 섹터)
  - 순위 1~10위를 등장 기사 수와 함께 **표**로 표시한다

### 2) 키워드 감성 분석 (긍정/부정)
- 설명: Top 10 각 키워드가 등장한 오늘자 기사 제목들을 감성사전으로 점수화해, 키워드별 감성 점수를 표 안에 함께 보여준다
- AI가 꼭 지켜야 할 규칙:
  - 사전은 **`SentiWord_info.json`(KnuSentiLex, 범용 14,843개) + `econNLPdic.txt`(재무 특화 60개)를 병합**해 사용한다
  - `econNLPdic.txt`의 극성은 **POS = +2, NEG = -2**로 환산한다
  - 두 사전에서 극성이 충돌하면 **`econNLPdic.txt`를 우선**한다
  - 감성어는 **형태소 단위로 매칭**한다. 문자열 부분 일치는 쓰지 않는다 (`"지지력"`의 '지지'가 부정어로 잡히는 오탐 방지)
  - 사전 점수(-2 ~ +2)만 사용하고, 외부 LLM이나 별도 모델로 감성을 추정하지 않는다
  - **제목 1건의 점수 = 그 제목에서 검출된 감성어 점수들의 평균** (합이 아니라 평균)
  - 키워드의 감성 점수 = 그 키워드가 등장한 제목들의 평균 점수
  - 감성어가 하나도 검출되지 않은 제목은 평균 계산에서 **제외**한다 (0점으로 넣지 않는다)
  - **+0.5 이상이면 긍정, -0.5 이하면 부정, 그 사이는 중립**으로 판정한다
  - 감성 점수는 소수점 둘째 자리까지 표시하고, **긍정/부정 라벨 컬럼은 따로 만들지 않고 점수 숫자에 색을 입힌다**
  - 감성어가 전혀 검출되지 않은 키워드는 0.00이 아니라 **`-`로 표시**한다
  - 표 오른쪽 위에 Top 10 전체 평균 감성 점수를 `평균값: +0.11` 형태로 표시한다
  - 표 아래에 작은 폰트로 판정 기준 범례를 넣는다
  - `SentiWord_info.json`(약 15,000줄)은 **직접 열어 읽지 말고** 코드로만 로드한다 (토큰 낭비 방지)

---

## 4. 이번에 절대 만들지 않을 기능 (욕심 버리기)
> 💡 AI에게 미리 "이건 안 만들 거야"라고 선언해야 엉뚱한 코딩을 안 합니다.
- 회원가입·로그인 기능 (개인화, 즐겨찾기, 알림 없음)
- 기사 본문 열람 화면 (키워드별 기사 **목록**은 만든다 — 키워드를 누르면 제목 + 원문 링크가 펼쳐진다)
- 경제 외 다른 카테고리(정치·사회·IT 등) 확장
- 과거 날짜 조회 및 키워드 추이 그래프 (오늘 하루 데이터만 다룬다)
- 외부 DB(PostgreSQL, Supabase 등) 연동 및 데이터 영구 저장 (파일 누적도 하지 않는다 — 시장 지표도 마찬가지로 DB 없이 매번 yfinance에서 다시 받아온다)
- LLM 기반 요약 및 감성 판정 (감성은 사전 규칙만 사용한다)
- 뉴스 본문 크롤링 (기사 제목만 사용한다)

**추가로 만든 것** (초기 범위 이후 사용자 요청으로 추가):
- 순위 변동 표시(▲▼), 키워드 클릭 시 기사 목록 펼치기
- 주요 시장 지표 8종(KOSPI·유로스탁스50·VIX·미국채10년물·원달러·엔원·유로원·위안원) — `yfinance`로 API 키 없이 수집, 자세한 내용은 PRD.md M7 참고

---

## 5. 디자인 느낌과 색상 고르기
- 전체적인 분위기: 금융 대시보드 느낌의 깔끔하고 절제된 스타일. 장식을 최소화하고 순위와 숫자가 바로 눈에 들어오도록 Top 10을 **하나의 표**로 나열한다 (컬럼: 순위 / 키워드 / 등장 기사 수 / 감성 점수)
- 메인 색상: 남색(네이비) 계열을 기본으로 한다. 감성 표시는 국내 증시 관례를 따라 **긍정=빨강, 부정=파랑, 중립=회색**으로 한다
- 화면 크기 제약: PC 브라우저 기준(가로 1200px 내외)으로 만들고, 모바일에서는 세로로 쌓이도록만 처리한다
